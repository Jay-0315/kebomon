{
  "name": "kebo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:user": "npm run dev --workspace @kebo/user-web",
    "build:user": "npm run build --workspace @kebo/user-web",
    "dev:admin": "npm run dev --workspace @kebo/admin-web",
    "build:admin": "npm run build --workspace @kebo/admin-web",
    "dev:api": "npm run dev --workspace @kebo/api",
    "build:api": "npm run build --workspace @kebo/api",
    "start:api": "npm run start --workspace @kebo/api",
    "prisma:generate": "npm run prisma:generate --workspace @kebo/api"
  }
}
